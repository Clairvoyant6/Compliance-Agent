# Core API
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0

# Persistence — SQLite now, Postgres-ready via DATABASE_URL env var
SQLAlchemy>=2.0.0
psycopg[binary]>=3.1.0 ; python_version >= "3.8"

# Scheduler for daily digest
APScheduler>=3.10.0

# Email (optional — falls back to stdout logs if RESEND_API_KEY unset)
resend>=0.8.0

# PDF export
reportlab>=4.0.0

# LLM providers (all optional — only needed when LLM_PROVIDER selected)
groq>=0.11.0
google-generativeai>=0.8.0
openai>=1.40.0
anthropic>=0.34.0
python-dotenv>=1.0.0
